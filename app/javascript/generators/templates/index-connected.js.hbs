import { PureComponent, createElement } from 'react';
import { connect } from 'react-redux';
// import Proptypes from 'prop-types';
// import { withRouter } from 'react-router';

import {{properCase name}}Component from './{{kebabCase name}}-component';
import actions from './{{kebabCase name}}-actions';
// import { {{camelCase name}}Selector } from './countries-select-selectors';

const mapStateToProps = (state) => ({
  state
});

class {{properCase name}}Container extends PureComponent {
  render() {
    return createElement({{properCase name}}Component, {
      ...this.props
    });
  }
}

{{properCase name}}Container.propTypes = {};


export { default as component } from './{{kebabCase name}}-component';
export { initialState } from './{{kebabCase name}}-reducers';
export { default as reducers } from './{{kebabCase name}}-reducers';
export { default as styles } from './{{kebabCase name}}-styles';
export { default as actions } from './{{kebabCase name}}-actions';

export default connect(mapStateToProps, actions)({{properCase name}}Container);
